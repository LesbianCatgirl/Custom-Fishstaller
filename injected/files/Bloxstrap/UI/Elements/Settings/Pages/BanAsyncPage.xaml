<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.BanAsyncPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels.Settings"
      d:DataContext="{d:DesignInstance Type=models:BanAsyncViewModel}"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="800"
      Title="BanAsyncPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <TextBlock Margin="0,0,0,4" Text="BanAsync" FontSize="18" FontWeight="SemiBold" />
        <TextBlock Margin="0,0,0,12" Text="clean Roblox traces, spoof a network adapter MAC, or randomize MachineGuid. run as admin for registry and adapter stuff." TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <ui:InfoBar Margin="0,0,0,8" IsOpen="True" Severity="Warning" Title="admin required" Message="MAC spoofing, MachineGuid changes, and prefetch cleanup need admin permissions." Visibility="{Binding ElevationWarningVisibility}" />

        <controls:OptionControl Header="trace cleanup" Description="deletes Roblox caches, logs, temp folders, prefetch entries, and Roblox HKCU registry data. Fishstrap settings stay untouched.">
            <ui:Button Appearance="Primary" Content="clean traces" Command="{Binding CleanTracesCommand}" />
        </controls:OptionControl>

        <controls:OptionControl Header="preserve in-game settings" Description="keeps GlobalBasicSettings XML files when cleaning traces.">
            <ui:ToggleSwitch IsChecked="{Binding PreserveInGameSettings, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Header="preserve FastFlags" Description="keeps ClientSettings JSON files found in Roblox folders.">
            <ui:ToggleSwitch IsChecked="{Binding PreserveFastFlags, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Header="include Studio folders" Description="also cleans Roblox Studio folders.">
            <ui:ToggleSwitch IsChecked="{Binding IncludeStudioFolders, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Header="clean Fishstrap versions" Description="deletes downloaded Roblox versions from Fishstrap so they get downloaded again next launch.">
            <ui:ToggleSwitch IsChecked="{Binding CleanFishstrapVersions, Mode=TwoWay}" />
        </controls:OptionControl>

        <TextBlock Text="MAC spoofing" FontSize="16" FontWeight="Medium" Margin="0,16,0,0" />
        <controls:OptionControl Header="network adapter" Description="pick a physical Ethernet or Wi-Fi adapter.">
            <StackPanel Orientation="Horizontal">
                <ComboBox Width="320" Padding="10,5,10,5" ItemsSource="{Binding Adapters}" DisplayMemberPath="DisplayLine" SelectedItem="{Binding SelectedAdapter, Mode=TwoWay}" />
                <ui:Button Margin="8,0,0,0" Content="refresh" Command="{Binding RefreshAdaptersCommand}" />
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl Header="MAC address" Description="leave this blank and hit shuffle to make one. OUI mirror keeps the adapter vendor prefix.">
            <StackPanel Orientation="Horizontal">
                <ui:TextBox Width="180" Margin="0,0,8,0" PlaceholderText="AA-BB-CC-11-22-33" Text="{Binding CustomMac, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ui:Button Margin="0,0,8,0" Content="shuffle" Command="{Binding ShuffleMacCommand}" />
                <ui:ToggleSwitch IsChecked="{Binding OuiMirror, Mode=TwoWay}" />
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl Header="current and original MAC" Description="{Binding OriginalMacFormatted}">
            <TextBlock Text="{Binding CurrentMacFormatted}" />
        </controls:OptionControl>

        <controls:OptionControl Header="persistent spoof" Description="when this is off, Fishstrap clears its tracked NetworkAddress overrides when it closes.">
            <ui:ToggleSwitch IsChecked="{Binding Persistent, Mode=TwoWay}" />
        </controls:OptionControl>

        <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
            <ui:Button Content="relaunch as admin" Command="{Binding RelaunchAdminCommand}" Visibility="{Binding ElevationWarningVisibility}" />
            <ui:Button Margin="8,0,0,0" Appearance="Primary" Content="spoof selected adapter" Command="{Binding SpoofCommand}" />
            <ui:Button Margin="8,0,0,0" Content="revert selected adapter" Command="{Binding RevertCommand}" />
        </StackPanel>

        <TextBlock Text="MachineGuid" FontSize="16" FontWeight="Medium" Margin="0,16,0,0" />
        <controls:OptionControl Header="acknowledgement" Description="MachineGuid is system-wide. make a restore point or backup before changing it.">
            <ui:ToggleSwitch IsChecked="{Binding MachineGuidAcknowledged, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Header="current MachineGuid" Description="{Binding MachineGuidBackupText}">
            <TextBlock Text="{Binding CurrentMachineGuid}" />
        </controls:OptionControl>

        <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
            <ui:Button Appearance="Primary" Content="randomize MachineGuid" Command="{Binding RandomizeMachineGuidCommand}" IsEnabled="{Binding MachineGuidActionsEnabled}" />
            <ui:Button Margin="8,0,0,0" Content="restore MachineGuid" Command="{Binding RestoreMachineGuidCommand}" />
        </StackPanel>

        <TextBlock Text="activity log" FontSize="16" FontWeight="Medium" Margin="0,16,0,0" />
        <ListBox Margin="0,8,0,0" MinHeight="160" MaxHeight="240" ItemsSource="{Binding ActivityLog}" />
        <ui:Button Margin="0,8,0,0" HorizontalAlignment="Left" Content="clear log" Command="{Binding ClearLogCommand}" />
    </StackPanel>
</ui:UiPage>
